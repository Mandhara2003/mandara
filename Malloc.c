#include<studio.h>
#include<stdlib.h>
Int main()
{
Int i,n,sum=0,*p
Printf("enter the size");
Scanf("%d",&n);
P=(int*)mallic(n*size of(int));
Printf("enter the elements");
for(i=0;i<n;i++)
{
Scanf("%d",p+i);
Sum=sum+(p+i);
}
Printf("\n sum=%d",sum);
free(p);
return 0;
}
